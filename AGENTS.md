# Agent Instructions

- Use the workspace scripts for build/test workflows, such as `python3 workspace.py build`.
- Do not create new workflow scripts unless the user asks for one or approves it first.
- Never run direct `ninja` commands.
- If a manual Chromium build command is needed, use the checkout's `autoninja`.
